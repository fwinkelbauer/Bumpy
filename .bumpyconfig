# Captures AssemblyVersion and AssemblyFileVersion in all AssemblyInfo.cs files
Source\**\AssemblyInfo.cs = (?<version>\d+\.\d+\.\d+\.\d+)

# Captures the version in all .nuspec files
NuSpec\**\*.nuspec = (?<version>\d+\.\d+\.\d+\.\d+)
